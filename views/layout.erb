<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>I'KNOW - Yapay Zeka Asistanı</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/app.js" defer></script>
</head>
<body>
    <header class="main-header">
        <div class="header-content">
            <a href="/" class="header-logo-link">
                <img src="/logo.png" alt="Logo" class="nav-logo">
                <span class="nav-title">I'KNOW</span>
            </a>
            <p class="nav-subtitle">Gemini AI Asistanı</p>
        </div>
    </header>

    <div class="app-wrapper">
        <aside class="sidebar">
            <a href="/" class="new-chat-btn">
                <span>➕</span> Yeni Sohbet
            </a>

            <div class="settings-card">
                <div class="settings-title">
                    <span>⚙️</span> Model Seçimi
                </div>
                <form action="/set_model" method="get" id="modelForm">
                    <select name="model" class="model-select">
                        <option value="gemini-3-flash-preview" <%= session[:model] == 'gemini-3-flash-preview' ? 'selected' : '' %>>Gemini 3 Flash (En İyi Seçenek)</option>
                        <option value="gemini-flash-latest" <%= session[:model] == 'gemini-flash-latest' ? 'selected' : '' %>>Gemini Flash Latest (Ücretsiz)</option>
                        <option value="gemini-2.5-flash" <%= session[:model] == 'gemini-2.5-flash' ? 'selected' : '' %>>Gemini 2.5 Flash (Yeni & Ücretsiz)</option>
                        <option value="gemini-2.5-flash-lite" <%= session[:model] == 'gemini-2.5-flash-lite' ? 'selected' : '' %>>Gemini 2.5 Flash-Lite (Çok Hızlı)</option>
                    </select>
                </form>
            </div>
            
            <div class="history-card">
                <div class="history-title">
                    <span>🕒 Geçmiş</span>
                    <% if session[:chat_history] && !session[:chat_history].empty? %>
                        <form action="/history/clear" method="post" style="display:inline;">
                            <button type="submit" class="clear-btn">Temizle</button>
                        </form>
                    <% end %>
                </div>
                <ul class="history-list">
                    <% if session[:chat_history] && !session[:chat_history].empty? %>
                        <% session[:chat_history].reverse.each do |chat| %>
                            <li class="history-item">
                                <div class="history-time"><%= chat[:timestamp] %></div>
                                <a href="/history/<%= chat[:id] %>" class="history-link" title="<%= chat[:prompt] %>">
                                    <%= chat[:prompt] %>
                                </a>
                            </li>
                        <% end %>
                    <% else %>
                        <li style="color: var(--text-muted); font-size: 0.8rem; text-align: center; padding: 1rem 0;">Henüz geçmiş yok.</li>
                    <% end %>
                </ul>
            </div>
        </aside>

        <main class="main-content glass-card">
            <%= yield %>
        </main>
    </div>

    <footer>
        <p>&copy; <%= Time.now.year %> Created by <a href="https://github.com/garalo" target="_blank" rel="noopener noreferrer">garalo</a></p>
    </footer>
</body>
</html>
